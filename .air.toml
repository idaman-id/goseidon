root = "."
tmp_dir = "storage"

[build]
  bin = "build\\file-service\\main.exe"
  cmd = "go build -o ./build/file-service/ ./cmd/file-service/main.go"
  delay = 1000
  exclude_dir = ["asset", "build", "doc", "storage", ".git"]
  exclude_file = []
  exclude_regex = []
  exclude_unchanged = false
  follow_symlink = false
  full_bin = ""
  include_dir = []
  include_ext = ["go"]
  kill_delay = "0s"
  log = "build-file-service.error.log"
  send_interrupt = false
  stop_on_error = true

[color]
  app = ""
  build = "yellow"
  main = "magenta"
  runner = "green"
  watcher = "cyan"

[log]
  time = false

[misc]
  clean_on_exit = false
